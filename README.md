# Galaxy Local Group Database

This repository contains the database for the Local Group of Galaxies.

## Database Design

The database schema is defined in the `schema.yaml` file. 
See also the database configuration in the `database.toml` file.

Additional documentation is available in the `docs` directory.